import 'package:flutter/material.dart';
	  
class StartMobile extends StatelessWidget {
	const StartMobile({super.key});
	  
	@override
	Widget build(BuildContext context) {
		return Container();
	}
}