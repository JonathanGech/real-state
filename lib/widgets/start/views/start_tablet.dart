import 'package:flutter/material.dart';
	  
class StartTablet extends StatelessWidget {
	const StartTablet({super.key});
	  
	@override
	Widget build(BuildContext context) {
		return Container();
	}
}